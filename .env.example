# Anthropic API key for Claude Agent SDK
ANTHROPIC_API_KEY=sk-ant-...
DEFAULT_MODEL=""
PROVIDER=Anthropic

# Web adapter (WebSocket — browser)
WEB_ENABLED=true
WEB_ALLOWED_DMS=*
WEB_ALLOWED_GROUPS=

# App adapter (WebSocket — mobile)
APP_ENABLED=true
APP_ALLOWED_DMS=*
APP_ALLOWED_GROUPS=

# Desktop adapter (WebSocket — Tauri desktop)
DESKTOP_ENABLED=false
DESKTOP_ALLOWED_DMS=*
DESKTOP_ALLOWED_GROUPS=

# Web UI (ui/web/.env.local)
GATEWAY_PASSWORD=changeme
NEXT_PUBLIC_GATEWAY_WS_URL=ws://localhost:4227/ws
NEXT_PUBLIC_APP_NAME=m-y.ai
